# Organizational cognition · diagrams

## Source (edit in tldraw offline)

| Board | File |
|-------|------|
| Asset vs pipe | `01-asset-vs-pipe.tldraw` |
| Export test | `02-export-test.tldraw` |
| File vs loop | `03-file-vs-loop.tldraw` |

Built via the desktop Canvas API (`helpers.createShape` + `createArrowBetweenShapes` + `saveDoc`).

## Published (what the essay loads)

| Image | Used in |
|-------|---------|
| `01-asset-vs-pipe.jpg` | §1 |
| `02-export-test.jpg` | §3 export test |
| `03-file-vs-loop.jpg` | §5 file vs loop |

Re-export: screenshot from offline (`api.getScreenshot`, canvas mode, large) or File → Export.

Legacy hand-rolled `.tldr` files (if any) are superseded by the `.tldraw` sources above.
